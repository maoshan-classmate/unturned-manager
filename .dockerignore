# ── 依赖 / 构建产物 ──
**/node_modules
**/dist
**/build

# ── 环境 / 密钥（禁止进镜像构建上下文）──
.env
.env.*
!.env.example

# ── 只读参考仓 + 测试数据（不参与构建）──
.research
test-servers
.test-install

# ── Docker compose bind-mount 宿主目录（运行时数据，不进构建上下文）──
# ⚠️ 必须用 / 锚定根目录：裸 `steamcmd` 会误排除 manager-server/src/modules/steamcmd 源码
/data
/opt
/steamcmd

# ── VCS ──
.git
.gitignore

# ── 日志 / 测试报告 ──
*.log
**/test-results
**/playwright-report
