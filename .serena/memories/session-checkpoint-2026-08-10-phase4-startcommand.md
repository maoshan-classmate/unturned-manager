ADR-0004 §4 Phase 4 在 2026-08-10 完成并提交 commit 7dc4f4e（9 文件 +638/-177）。解决 ServerControlCard 顶部「默认命令」硬编码 span + 底下「启动命令」按钮误指 LaunchCommandsDialog（Commands.dat 编辑）的错位 UI——Phase 4 把硬编码改成 inline Input + 保存按钮（真实编辑 startCommand），下方入口文案调整为「Commands.dat 参数」区分。后端：shared/schemas/server.schema.ts 的 ConfigureServerSchema 新增 startCommand 字段（min1/max2048/optional）；manager-server/src/modules/settings/settingsStorage.ts 新增明文 K-V 三件套 getStartCommand/setStartCommand/deleteStartCommand（key startCommand:<id>，复用 settings 表 value_enc 列明文写入不走 AES-GCM）；manager-server/src/modules/server/ServerManager.ts 在 loadServersFromDisk 调 restoreStartCommand 恢复、新增 restoreStartCommand 私有方法、createServer 在 config.startCommand 传入时落库、configureServer patch.startCommand 走 setStartCommand 落库、removeServer 同步 deleteStartCommand 清 K-V；新增 5 单测 + 2 PATCH 集成测覆盖。前端：manager-web/src/schemas/serverSetup.ts 新增 startCommandSchema；manager-web/src/hooks/useServer.ts 的 ServerInfo 加 startCommand 字段 + 新增 updateServer(id, patch) 调 PATCH /servers/:id；manager-web/src/components/server-setup/ServerControlCard.tsx 硬编码 span 改 Input + 保存按钮（inline 编辑 startCommand）+ 复用 useServer().updateServer，下 LaunchCommandsDialog 入口文案改「Commands.dat 参数」；manager-web/src/pages/ServerSetupPage.tsx 传 startCommand 接线。验证：后端单测 172/172（165+5+2），后端 e2e 26/26，前端单测 29/29，typecheck 0 错误，prettier 干净。关键方案纠正：Plan 原设计「createServer 必落 buildStartCommand 兜底生成的 startCommand」改回 createServer 仅在用户显式传 startCommand 时落库，首次兜底仍由 startPty 现有逻辑按需探测（语义对齐 start = 检测启动）。
