小功能判定 hook 时机改造（2026-08-15 完成）：small-feature-detect.sh 从 PostToolUse（每次写文件触发）移到 Stop 事件（本轮工作收尾判定一次）。提醒通道用 hookSpecificOutput.additionalContext（非阻塞反馈，界面显示 Stop hook feedback 而非 error；实测 exit 2 和 JSON block 在 Stop 下都显示 Stop hook error）。prompt_id 同轮去重（state 文件）。threshold.json exclude_paths 加 .claude/hooks/（hook 脚本改动不计入小功能判定）。PostToolUse 挂载已删。规格文档：claudedocs/workflow_small-feature-detect-stop.md。