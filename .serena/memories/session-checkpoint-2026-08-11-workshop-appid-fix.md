创意工坊下载失败根因修复（2026-08-11，提交 05c338c）。

根因：下载创意工坊内容时误用了服务端工具的应用编号 `1110390`，该编号名下没有创意工坊内容，只能拿到元数据缓存、拿不到实际内容——这是实机测试中「提示下载成功但文件没落盘」的总根因。

修正：创意工坊全链路切换为游戏本体编号 `304930`，涉及下载命令、落盘校验、清单解析、应用生效、删除、路径解析注释，以及 4 个测试夹具和 7 份文档。

未改动（原本就正确）：安装与更新服务端二进制、查询服务端版本仍用 `1110390`；网页接口查询本来就用 `304930`。

配套重构——应用编号全局唯一真源：
- 新建 `shared/constants.ts`，导出两个编号常量
- 消除 5 处后端手写字面量与前端 1 处硬编码

**应用编号铁律**：
- 装 / 更新服务端二进制 → `1110390`
- 创意工坊下载、落盘、清单、网页接口 → `304930`
- 两者不可互换，`shared/constants.ts` 是唯一真源

验证：前后端类型检查零错误、单测 186 全绿。

相关：[[e2e-gap-lesson-mods-downloaded]]、[[session-checkpoint-2026-08-10-linux-uat-bugfix]]
