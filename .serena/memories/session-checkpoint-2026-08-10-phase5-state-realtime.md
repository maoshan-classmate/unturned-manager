ADR-0004 §4 Phase 5 在 2026-08-10 完成并提交 commit d256fa0（3 文件 +198/-77）。解决 Dashboard/实例列表状态显示与后端状态机不同步问题——点启动后状态卡在 STOPPED 等、点停止后状态卡在 RUNNING 不刷新。Phase 5 把 WebSocketContext 从「只管连接」升级为「连接 + 事件订阅总线」，useServer 订阅 state_change 事件实时更新 server.state（不走 GET /servers 重拉）。具体：manager-web/src/contexts/WebSocketContext.tsx 增加 ServerEventMessage 接口 + subscribe(listener) 接口 + listenersRef 集合（ref 模式避免 React 重渲）+ ws.onmessage 广播给所有 listener；manager-web/src/hooks/useServer.ts 在 useServer 内新增 useEffect 订阅 state_change 事件，在 setServers(prev.map) 中只更新对应 serverId 的 state 字段；manager-web/src/pages/DashboardPage.tsx 删掉 handleAction 后的 refresh()（避免覆盖 WS 已更新的 state）+ 删掉「A2S 查询需启动后生效」过时文案（改为「需 Pty 终端敲 Players 命令查看」）。后端零改动——ServerManager.transition 在 spawn/exit 时已广播 state_change 事件（ServerManager.ts:904），前端只是补上订阅链路。验证：后端单测 172/172，后端 e2e 26/26，前端单测 29/29，typecheck 0 错误，prettier 干净。关键洞察：后端 task 调研发现 PTY 已在 Phase 2 替换 A2S 驱动状态机，WS 事件总线已就绪但前端没接线——Phase 5 纯前端工作量。
