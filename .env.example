# unturned-manager Docker Compose 环境变量模板
# 复制为 .env 并填写真实值：cp .env.example .env
# compose 自动读取【同目录】的 .env 宿主文件，映射进容器（见 docker-compose.yml environment 块）。

# ── 密钥/密码（留空 = 用 compose 默认值可快速启动；生产务必覆盖）──
# ⚠️ 不覆盖时 compose 用固定默认值（与 manager-server/.env 开发值一致）：
#   JWT_SECRET     默认 = 固定 hex——所有未覆盖部署共享同一密钥，可伪造 JWT，生产必须改
#   ENCRYPTION_KEY 默认 = 固定 base64——AES-GCM 加密密钥，生产必须改
#   ADMIN_PASSWORD 默认 = 123456——首启种子密码，生产必须改
# 覆盖前建议生成强随机值：
#   JWT_SECRET:     openssl rand -hex 32
#   ENCRYPTION_KEY: openssl rand -base64 32
JWT_SECRET=
ENCRYPTION_KEY=
ADMIN_PASSWORD=

# ── 可选（带默认值，可省略）──
# 面板监听端口 / 地址
SERVER_PORT=3001
HOST=0.0.0.0
# SteamCMD 安装路径（镜像默认烘焙到 /opt/steamcmd；改镜像布局时覆盖）
STEAMCMD_DIR=/opt/steamcmd
# pino 日志级别：info / debug / warn / error
LOG_LEVEL=info
# CORS 来源（同源部署留 * 即可）
CORS_ORIGIN=*
